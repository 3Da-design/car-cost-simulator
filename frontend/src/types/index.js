/**
 * 共通の JSDoc 型（グローバルに参照されるオブジェクトの形）
 * @typedef {{ type: 'error' | 'success', text: string }} ImportMessage
 * @typedef {{ id: string, label: string }} ModelOption
 * @typedef {{ id: string, label: string, icon: string, disabled?: boolean }} NavItem
 */

export {}
